micronaut:
  application:
    name: micronautEmbeddedWorker
camunda:
  admin-user:
    id: admin
    password: admin
  generic-properties:
    properties:
      initialize-telemetry: true
  webapps:
    enabled: true
  rest:
    enabled: true
  filter:
    create: "All tasks"
  external-client.base-url: http://localhost:8080/engine-rest
